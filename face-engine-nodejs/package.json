{
    "name": "face-engine",
    "version": "0.0.1",
    "description": "Node.js module to publish face related jobs to redis channels and listen to job-done messages.",
    "main": "index.js",
    "scripts": {
        "start": "./index.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "MIT",
    "engines": {
        "node": ">=0.6.6",
        "npm": ">=1.1"
    },
    "dependencies": {
        "gridfs-stream": "0.4.0",
        "redis": ">=0.8.6"
    }
}
